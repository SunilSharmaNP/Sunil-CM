# Sample Configuration File for Enhanced MERGE-BOT
# Copy this file to config.env and fill in your values

# ===== REQUIRED CONFIGURATION =====
# These variables are mandatory for the bot to function

# Telegram API Credentials (get from https://my.telegram.org)
API_ID=your_telegram_api_id
TELEGRAM_API=your_telegram_api_id          # Alternative name (same as API_ID)
API_HASH=your_telegram_api_hash

# Bot Token (get from @BotFather)
BOT_TOKEN=your_bot_token_here

# Bot Owner Information
OWNER=your_telegram_user_id                # Your Telegram user ID (numeric)
OWNER_USERNAME=your_telegram_username      # Your Telegram username (without @)
PASSWORD=your_bot_access_password          # Password for users to access the bot

# ===== DATABASE CONFIGURATION =====
# MongoDB database for user management and settings (optional but recommended)
DATABASE_URL=your_mongodb_connection_string

# ===== LOGGING AND STORAGE =====
# Log Channel ID for storing merged videos (optional)
LOGCHANNEL=-100your_log_channel_id

# ===== PREMIUM FEATURES =====
# Premium Telegram account session string for 4GB file uploads (optional)
USER_SESSION_STRING=your_premium_session_string

# ===== CLOUD STORAGE INTEGRATION =====
# GoFile.io API token for external file sharing (optional)
GOFILE_TOKEN=your_gofile_token

# Google Drive folder ID for cloud uploads (optional, requires rclone setup)
GDRIVE_FOLDER_ID=root

# ===== DOWNLOAD CONFIGURATION =====
# Directory for temporary file downloads
DOWNLOAD_DIR=downloads

# ===== PERFORMANCE SETTINGS =====
# Throttle time to prevent FloodWait errors (in seconds)
EDIT_THROTTLE_SECONDS=4.0

# Maximum concurrent downloads
MAX_CONCURRENT_DOWNLOADS=3

# Download and upload timeouts (in seconds)
DOWNLOAD_TIMEOUT=300
UPLOAD_TIMEOUT=300

# ===== FFMPEG CONFIGURATION =====
# FFmpeg processing settings for video encoding
FFMPEG_PRESET=fast                         # fast, medium, slow, veryfast, slower
VIDEO_CRF=23                              # Constant Rate Factor (18-28, lower=better quality)
AUDIO_BITRATE=192k                        # Audio bitrate
VIDEO_CODEC=libx264                       # Video codec (libx264, libx265)
AUDIO_CODEC=aac                           # Audio codec (aac, mp3, flac)

# ===== UI AND PROGRESS SETTINGS =====
# Progress bar appearance
PROGRESS_BAR_LENGTH=20
FINISHED_PROGRESS_STR=█
UN_FINISHED_PROGRESS_STR=░

# ===== ADVANCED FEATURES =====
# Enable/disable advanced features (true/false)
ENABLE_COMPRESSION=true                   # Allow video compression
AUTO_DELETE_FILES=true                    # Auto-delete temp files after processing
ENABLE_STATS_LOGGING=true                # Log detailed statistics
DEBUG_MODE=false                          # Enable debug logging
ENABLE_RATE_LIMITING=true                # Enable rate limiting protection

# ===== SECURITY SETTINGS =====
# Login attempt limits and lockout
MAX_LOGIN_ATTEMPTS=3                      # Max failed login attempts
LOCKOUT_DURATION=300                      # Lockout duration in seconds (5 minutes)

# ===== LOGGING CONFIGURATION =====
# Logging level and file settings
LOG_LEVEL=INFO                            # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE_SIZE=50000000                    # Max log file size in bytes (50MB)
LOG_BACKUP_COUNT=10                       # Number of log backup files

# ===== CACHING SETTINGS =====
# Performance caching options
ENABLE_CACHING=true                       # Enable in-memory caching
CACHE_EXPIRY=3600                         # Cache expiry time in seconds (1 hour)
MAX_CACHE_SIZE=100                        # Maximum cached items

# ===== WEBHOOK CONFIGURATION (Optional) =====
# For webhook-based deployments (Heroku, Vercel, etc.)
ENABLE_WEBHOOK=false
WEBHOOK_HOST=your_app_domain.com
WEBHOOK_PORT=8080
WEBHOOK_URL_BASE=https://your_app_domain.com
WEBHOOK_URL_PATH=/webhook
